<resources>

    <!-- Base application theme. -->
    <style name="AppThemeWeather" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <style name="Toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="WeatherTitle">
        <item name="android:layout_marginStart">@dimen/weather_title_margin_start</item>
        <item name="android:textSize">@dimen/weather_title_text_size</item>
        <item name="android:layout_weight">1</item>
        <item name="android:height">48dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="WeatherValue">
        <item name="android:layout_marginEnd">@dimen/weather_value_margin_end</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">end</item>
        <item name="android:textSize">@dimen/weather_value_text_size</item>
        <item name="android:height">48dp</item>

    </style>
</resources>
